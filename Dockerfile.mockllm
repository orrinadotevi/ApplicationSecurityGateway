FROM python:3.11-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi==0.111.0 uvicorn[standard]==0.30.1 pydantic==2.8.2
COPY scripts /app/scripts
CMD ["uvicorn", "scripts.mock_llm:app", "--host", "0.0.0.0", "--port", "8001"]
